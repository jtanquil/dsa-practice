#ifndef BINARYSEARCH_H
#define BINARYSEARCH_H

#define len(arr) (int) sizeof(arr) / (int) sizeof(int)

int iterative_binary_search(int *, int, int);
int recursive_binary_search(int *, int, int, int);

#endif